# Dossier publié sur GitHub Pages

Contenu généré par `scripts/build-vitrine.sh` — ne pas éditer à la main.

```bash
VITRINE_OUT=docs bash scripts/build-vitrine.sh
git add docs/
git commit --trailer "Co-authored-by: Cursor <cursoragent@cursor.com>" -m "Rebuild vitrine GitHub Pages"
```

Sur GitHub : **Settings ? Pages ? Build from branch `main`, folder `/docs`**.
